# Local dataset / scoring helpers (CPU-friendly)
pyyaml>=6.0
datasets>=2.19.0

# GPU training stack — install on the Linux training host (Udutech / Colab)
# torch: install the CUDA build that matches the host, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0
safetensors>=0.4.0

# ADTC profiler (separate install recommended):
#   pip install "git+https://github.com/Africa-Deep-Tech-Foundation/adtc-profiler.git"
# Requires Python >= 3.11 and llama-bench on PATH.
